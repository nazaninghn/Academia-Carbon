# 🔐 راهنمای لاگین / Login Guide

## ✅ مشکل حل شد! / Problem Fixed!

مشکل Server Error 500 در صفحه لاگین حل شد.

---

## 🔧 تغییرات انجام شده / Changes Made

### 1. **Custom Authentication Backend** 
ایجاد یک backend سفارشی برای احراز هویت با ایمیل:
- فایل: `ghg/backends.py`
- امکان لاگین با ایمیل به جای username

### 2. **بهبود Form لاگین**
- حذف متد `clean_username` که باعث خطا می‌شد
- ساده‌سازی فرم لاگین

### 3. **بهبود View لاگین**
- استفاده مستقیم از `authenticate()` با ایمیل
- پیام‌های خطای بهتر

### 4. **تنظیمات Django**
اضافه شدن به `settings.py`:
```python
AUTHENTICATION_BACKENDS = [
    'ghg.backends.EmailBackend',  # Custom email backend
    'django.contrib.auth.backends.ModelBackend',  # Fallback
]
```

---

## 👤 کاربر تست / Test User

برای تست سیستم، یک کاربر آماده شده:

```
📧 Email: test@example.com
🔑 Password: test123456
```

---

## 🚀 نحوه استفاده / How to Use

### 1. اجرای سرور
```bash
python manage.py runserver
```

### 2. باز کردن صفحه لاگین
```
http://127.0.0.1:8000/en/login/
```

### 3. وارد کردن اطلاعات
- **Email**: test@example.com
- **Password**: test123456

### 4. کلیک روی "Sign In"

---

## 📝 ایجاد کاربر جدید / Create New User

### روش 1: از طریق صفحه Sign Up
1. برو به: http://127.0.0.1:8000/en/signup/
2. ایمیل و رمز عبور خود را وارد کنید
3. روی "Create Account" کلیک کنید

### روش 2: از طریق Django Admin
```bash
python manage.py createsuperuser
```

### روش 3: با اسکریپت
```bash
python create_test_user.py
```

---

## 🔍 بررسی مشکلات / Troubleshooting

### اگر باز هم خطا دارید:

#### 1. بررسی لاگ سرور
در terminal که سرور اجرا شده، خطاها را ببینید.

#### 2. بررسی دیتابیس
```bash
python manage.py migrate
```

#### 3. ایجاد کاربر جدید
```bash
python create_test_user.py
```

#### 4. پاک کردن Cache مرورگر
- Ctrl + Shift + Delete
- پاک کردن Cookies و Cache

#### 5. بررسی ماژول‌ها
```bash
pip install -r requirements.txt
```

---

## 📦 ماژول‌های نصب شده / Installed Packages

```
Django==5.2.8
dj-database-url==3.0.1
python-decouple==3.8
whitenoise==6.11.0
```

---

## 🎯 ویژگی‌های جدید / New Features

### ✅ لاگین با ایمیل
- دیگر نیازی به username نیست
- فقط ایمیل و رمز عبور کافی است

### ✅ پیام‌های بهتر
- پیام خوش‌آمدگویی پس از لاگین
- پیام‌های خطای واضح‌تر

### ✅ امنیت بیشتر
- استفاده از Django Authentication System
- رمزگذاری رمز عبور با bcrypt

---

## 🔐 امنیت / Security

### رمز عبور قوی:
- حداقل 8 کاراکتر
- ترکیب حروف و اعداد
- عدم استفاده از اطلاعات شخصی

### تنظیمات امنیتی:
```python
# در production این‌ها را فعال کنید:
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

---

## 📞 پشتیبانی / Support

اگر مشکلی داشتید:
1. لاگ‌های سرور را بررسی کنید
2. Browser Console را چک کنید (F12)
3. مطمئن شوید تمام ماژول‌ها نصب شده‌اند

---

## ✨ نکات مهم / Important Notes

1. **Development Mode**: سرور در حالت توسعه است، برای production تنظیمات را تغییر دهید
2. **Database**: از SQLite استفاده می‌شود، برای production از PostgreSQL استفاده کنید
3. **Static Files**: با `collectstatic` فایل‌های استاتیک را جمع‌آوری کنید

---

**تاریخ:** 2025-01-24  
**نسخه:** 1.0.0  
**وضعیت:** ✅ کار می‌کند
